server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/arkadaslikdb
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true #Girintiler önemli!!